<%- include('../partials/header') %>
<%- include('../partials/sidebar') %>

<div class="main-content">
    <div class="container-fluid py-4">
        
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <div>
                <h1 class="h2 mb-1"><i class="fas fa-network-wired text-primary me-2"></i> Worker Node Status</h1>
                <p class="text-muted mb-0">Detailed view of individual worker nodes and their health.</p>
            </div>
            <button class="btn btn-outline-secondary fw-bold shadow-sm">
                <i class="fas fa-sync-alt me-2"></i> Refresh Nodes
            </button>
        </div>

        <div class="card shadow-sm border-0">
            <div class="card-body text-center py-5">
                <i class="fas fa-server text-muted mb-3" style="font-size: 3rem;"></i>
                <h4 class="text-dark fw-bold">Node Status Module</h4>
                <p class="text-muted">This module is currently connected to placeholder UI. Data will be populated from the backend API soon.</p>
            </div>
        </div>

    </div>
</div>

<%- include('../partials/footer') %>
